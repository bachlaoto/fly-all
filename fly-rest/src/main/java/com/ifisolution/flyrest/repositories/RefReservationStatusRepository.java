package com.ifisolution.flyrest.repositories;

import com.ifisolution.flyrest.domain.RefReservationStatus;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RefReservationStatusRepository extends JpaRepository<RefReservationStatus, Long> {
}
